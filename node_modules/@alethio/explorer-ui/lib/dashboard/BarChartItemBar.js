import * as React from "react";
import styled, { css } from "../styled-components";
import { BarChartItemRoot } from "./BarChartItem";
const BarChartItemBarRoot = styled("div") `
    height: 100%;
    box-sizing: border-box;
    border: 2px solid ${props => (props.active ?
    props.theme.colors.blockColorCode :
    props.theme.colors.blockListItem)};
    background-color: ${props => (props.active ?
    props.theme.colors.blockColorCode :
    props.theme.colors.blockListItem)};

    ${props => !props.disabled && !props.active ? css `
    ${BarChartItemRoot}:hover & {
        border: 2px solid ${props.theme.colors.blockColorCode};
        background-color: transparent;
    }
    ` : ``}
    ${props => props.disabled ? css `
    opacity: .5;
    ` : ``}
`;
export const BarChartItemBar = (props) => (React.createElement(BarChartItemBarRoot, Object.assign({}, props)));
//# sourceMappingURL=BarChartItemBar.js.map