import * as React from "react";
import { Number } from "@alethio/ui/lib/data/Number";
import { GridLink } from "../../GridLink";
export class BlockLinkRenderer {
    constructor(locale, gridFieldDataGetter) {
        this.locale = locale;
        this.gridFieldDataGetter = gridFieldDataGetter;
    }
    render(f) {
        let blockNumber = this.gridFieldDataGetter(f);
        return (React.createElement(GridLink, { to: `page://aleth.io/block?blockNumber=${blockNumber}` },
            React.createElement(Number, { locale: this.locale, value: blockNumber })));
    }
}
//# sourceMappingURL=BlockLinkRenderer.js.map