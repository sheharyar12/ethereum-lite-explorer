import * as React from "react";
import { Hash } from "@alethio/ui/lib/data/Hash";
import { GridLink } from "../../GridLink";
export class TxLinkRenderer {
    constructor(gridFieldDataGetter) {
        this.gridFieldDataGetter = gridFieldDataGetter;
    }
    render(f) {
        let hash = this.gridFieldDataGetter(f);
        return (React.createElement(GridLink, { to: `page://aleth.io/tx?txHash=${hash}` },
            React.createElement(Hash, null, hash)));
    }
}
//# sourceMappingURL=TxLinkRenderer.js.map