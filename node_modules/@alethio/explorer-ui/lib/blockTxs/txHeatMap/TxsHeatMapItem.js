import * as React from "react";
import { DomNodeProxy } from "@alethio/ui/lib/util/react/DomNodeProxy";
import { Link } from "plugin-api/component/Link";
import { TxBubble } from "./TxBubble";
export class TxsHeatMapItem extends React.PureComponent {
    constructor() {
        super(...arguments);
        this.handleMouseEnter = () => {
            this.props.onMouseEnter(this.props.tx);
        };
        this.handleMouseLeave = () => {
            this.props.onMouseLeave(this.props.tx);
        };
    }
    render() {
        return (React.createElement(DomNodeProxy, { onMount: el => {
                el.addEventListener("mouseenter", this.handleMouseEnter);
                el.addEventListener("mouseleave", this.handleMouseLeave);
            }, onUnmount: el => {
                el.removeEventListener("mouseenter", this.handleMouseEnter);
                el.removeEventListener("mouseleave", this.handleMouseLeave);
            } },
            React.createElement(Link, { to: `page://aleth.io/tx?txHash=${this.props.tx.hash}` },
                React.createElement(TxBubble, { size: this.props.bubbleSize, highlightThreshold: this.props.highlightThreshold }))));
    }
}
//# sourceMappingURL=TxsHeatMapItem.js.map