import * as React from "react";
import { Bubble } from "@alethio/ui/lib/data/vis/Bubble";
import { ThemeContext } from "plugin-api/ThemeContext";
export const TxBubble = ({ size, highlightThreshold }) => (React.createElement(ThemeContext.Consumer, null, (theme) => React.createElement(Bubble, { size: size, wrapperSize: theme.spacing.bubbleWrapperSize, highlightThreshold: highlightThreshold, backgroundColor: theme.colors.txColorCode })));
//# sourceMappingURL=TxBubble.js.map