import * as React from "react";
import { HashLinkValueBox } from "../HashLinkValueBox";
export const TxHashBox = ({ children, variant, noLink, noTooltip }) => (React.createElement(HashLinkValueBox, { colors: (theme) => ({
        background: theme.colors.txColorCode,
        text: theme.colors.txBoxText
    }), variant: variant, linkTo: !noLink ? `page://aleth.io/tx?txHash=${children}` : void 0, noTooltip: noTooltip }, children));
//# sourceMappingURL=TxHashBox.js.map