import * as React from "react";
import { ValueBox } from "@alethio/ui/lib/layout/content/box/ValueBox";
export class UncleNumberBox extends React.Component {
    render() {
        let { children, variant } = this.props;
        return React.createElement(ValueBox, { colors: (theme) => ({
                background: theme.colors.unclesBoxBg,
                text: theme.colors.unclesBoxText,
                border: theme.colors.unclesBoxBorder
            }), variant: variant },
            "#",
            children);
    }
}
//# sourceMappingURL=UncleNumberBox.js.map