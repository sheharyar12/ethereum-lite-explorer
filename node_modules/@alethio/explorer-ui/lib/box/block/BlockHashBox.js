import * as React from "react";
import { HashLinkValueBox } from "../HashLinkValueBox";
export class BlockHashBox extends React.Component {
    render() {
        let { children, linkTo } = this.props;
        return (React.createElement(HashLinkValueBox, { colors: (theme) => ({
                background: theme.colors.blockColorCode,
                text: theme.colors.blockBoxText
            }), linkTo: linkTo }, children));
    }
}
//# sourceMappingURL=BlockHashBox.js.map