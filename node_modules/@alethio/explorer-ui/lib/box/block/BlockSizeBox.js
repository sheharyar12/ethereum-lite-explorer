import * as React from "react";
import { ValueBox } from "@alethio/ui/lib/layout/content/box/ValueBox";
import { ByteSize } from "@alethio/ui/lib/data/ByteSize";
export const BlockSizeBox = ({ bytes, locale, translations }) => (React.createElement(ValueBox, null,
    React.createElement(ByteSize, { format: "%d " + translations.bytes, locale: locale }, bytes)));
//# sourceMappingURL=BlockSizeBox.js.map