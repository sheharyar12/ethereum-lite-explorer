import * as React from "react";
import { HashLinkValueBox } from "../HashLinkValueBox";
export const ParentHashBox = ({ children, linkTo }) => (React.createElement(HashLinkValueBox, { colors: (theme) => ({
        background: theme.colors.parentHashBoxBg,
        text: theme.colors.parentHashBoxText
    }), linkTo: linkTo }, children));
//# sourceMappingURL=ParentHashBox.js.map