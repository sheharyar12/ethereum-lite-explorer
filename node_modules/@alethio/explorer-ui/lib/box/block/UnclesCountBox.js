import * as React from "react";
import { ValueBox } from "@alethio/ui/lib/layout/content/box/ValueBox";
import { Number } from "@alethio/ui/lib/data/Number";
export class UnclesCountBox extends React.Component {
    render() {
        return (React.createElement(ValueBox, { colors: (theme) => ({
                background: theme.colors.unclesBoxBg,
                text: theme.colors.unclesBoxText,
                border: theme.colors.unclesBoxBorder
            }), variant: this.props.variant },
            React.createElement(Number, { locale: this.props.locale, value: this.props.children })));
    }
}
//# sourceMappingURL=UnclesCountBox.js.map