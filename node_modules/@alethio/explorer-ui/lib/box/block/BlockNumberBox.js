import * as React from "react";
import { ValueBox } from "@alethio/ui/lib/layout/content/box/ValueBox";
import { Link } from "plugin-api/component/Link";
export class BlockNumberBox extends React.Component {
    render() {
        let { children, variant, noLink } = this.props;
        let box = React.createElement(ValueBox, { colors: (theme) => ({
                background: theme.colors.blockColorCode,
                text: theme.colors.blockBoxText
            }), variant: variant },
            "#",
            children);
        return !noLink ? React.createElement(Link, { to: `page://aleth.io/block?blockNumber=${children}` }, box) : box;
    }
}
//# sourceMappingURL=BlockNumberBox.js.map