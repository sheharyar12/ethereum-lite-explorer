import * as React from "react";
import { Link } from "plugin-api/component/Link";
import { HashValueBox } from "@alethio/ui/lib/data/box/HashValueBox";
export class HashLinkValueBox extends React.Component {
    render() {
        let { children, linkTo } = this.props;
        let box = React.createElement(HashValueBox, { variant: this.props.variant, colors: this.props.colors, Icon: this.props.Icon, noTooltip: this.props.noTooltip }, children);
        return linkTo ? React.createElement(Link, { to: linkTo }, box) : box;
    }
}
//# sourceMappingURL=HashLinkValueBox.js.map