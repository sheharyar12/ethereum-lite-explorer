import * as React from "react";
import { ValueBox } from "@alethio/ui/lib/layout/content/box/ValueBox";
import { Number } from "@alethio/ui/lib/data/Number";
export class AccountMinedBlocksBox extends React.Component {
    render() {
        let { children, variant, locale } = this.props;
        return (React.createElement(ValueBox, { colors: (theme) => ({
                background: theme.colors.blockColorCode,
                text: theme.colors.blockBoxText
            }), variant: variant },
            React.createElement(Number, { locale: locale, value: children })));
    }
}
//# sourceMappingURL=AccountMinedBlocksBox.js.map