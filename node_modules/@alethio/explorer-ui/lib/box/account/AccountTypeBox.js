import * as React from "react";
import { ValueBox } from "@alethio/ui/lib/layout/content/box/ValueBox";
export const AccountTypeBox = ({ children }) => (React.createElement(ValueBox, { colors: (theme) => ({
        background: theme.colors.accountTypeBoxBackground,
        text: theme.colors.accountTypeBoxText
    }) }, children));
//# sourceMappingURL=AccountTypeBox.js.map