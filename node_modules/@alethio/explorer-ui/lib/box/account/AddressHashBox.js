import * as React from "react";
import { HashLinkValueBox } from "../HashLinkValueBox";
export const AddressHashBox = ({ children, variant, Icon, noLink }) => (React.createElement(HashLinkValueBox, { colors: (theme) => ({
        background: theme.colors.addressHashBoxBg,
        text: theme.colors.valueBox.primary.text
    }), variant: variant, Icon: Icon, linkTo: !noLink ? `page://aleth.io/account?accountHash=${children}` : void 0 }, children));
//# sourceMappingURL=AddressHashBox.js.map