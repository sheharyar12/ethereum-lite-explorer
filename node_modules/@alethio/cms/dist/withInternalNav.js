import React from "react";
import { withRouter } from "react-router";
import { LinkContext } from "./component/LinkContext";
import { InternalNav } from "./InternalNav";
import { InternalUrlResolver } from "./InternalUrlResolver";
class Wrapper extends React.Component {
    render() {
        let { C, childProps, history } = this.props;
        return React.createElement(LinkContext.Consumer, null, ({ pages }) => React.createElement(C, Object.assign({ internalNav: new InternalNav(new InternalUrlResolver(pages), history) }, childProps)));
    }
}
const WrapperWithRouter = withRouter(Wrapper);
export const withInternalNav = (C) => (props) => React.createElement(WrapperWithRouter, { C: C, childProps: props });
//# sourceMappingURL=withInternalNav.js.map