import { InternalUrlResolver } from "./InternalUrlResolver";
import { History } from "history";
export declare class InternalNav {
    private internalUriResolver;
    private history;
    constructor(internalUriResolver: InternalUrlResolver, history: History);
    goTo(pageUri: string): boolean;
    resolve(pageUri: string): string | false;
}
//# sourceMappingURL=InternalNav.d.ts.map