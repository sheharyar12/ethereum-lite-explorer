export class InternalNav {
    constructor(internalUriResolver, history) {
        this.internalUriResolver = internalUriResolver;
        this.history = history;
    }
    goTo(pageUri) {
        let resolvedUrl = this.internalUriResolver.resolve(pageUri);
        if (resolvedUrl) {
            this.history.push(resolvedUrl);
        }
        return !!resolvedUrl;
    }
    resolve(pageUri) {
        return this.internalUriResolver.resolve(pageUri);
    }
}
//# sourceMappingURL=InternalNav.js.map