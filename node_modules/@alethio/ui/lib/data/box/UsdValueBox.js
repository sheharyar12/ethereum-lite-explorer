import * as React from "react";
import { ValueBox } from "../../layout/content/box/ValueBox";
export class UsdValueBox extends React.Component {
    render() {
        let colors = this.props.colors || "secondary";
        return (React.createElement(ValueBox, { variant: this.props.variant, colors: colors }, this.formatUsd(this.props.value, this.props.locale)));
    }
    formatUsd(value, locale) {
        return value.toLocaleString(locale, {
            currency: "USD",
            currencyDisplay: "symbol",
            style: "currency"
        });
    }
}
//# sourceMappingURL=UsdValueBox.js.map